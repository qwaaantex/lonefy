import 'package:flutter/material.dart';

class LoginChildrenText extends StatelessWidget {
  const LoginChildrenText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("lonefy", style: Theme.of(context).textTheme.titleMedium,);
  }
}