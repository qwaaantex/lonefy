import 'package:flutter/cupertino.dart';

class SongInfoColumn extends StatelessWidget {
  const SongInfoColumn({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
      
    ],);
  }
}